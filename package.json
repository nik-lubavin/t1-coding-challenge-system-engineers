{
  "name": "t1-coding-challenge",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "start": "docker-compose up -d",
    "stop": "docker-compose down",
    "prune": "docker system prune --all --volumes --force",
    "logs:k": "docker-compose logs -f kafka-producer",
    "logs:c": "docker-compose logs -f calculation-service"
  }
}
