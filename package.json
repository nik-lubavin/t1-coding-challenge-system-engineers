{
  "name": "t1-coding-challenge",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "start": "docker-compose up -d",
    "prune": "docker system prune --all --volumes --force",
    "logs": "docker-compose logs -f kafka-producer"
  }
}
